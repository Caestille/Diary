<Window
    x:Class="Diary.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ModernThemables.Controls;assembly=ModernThemables"
    xmlns:viewmodels="clr-namespace:Diary.ViewModels"
    xmlns:views="clr-namespace:Diary.Views"
    x:Name="Window"
    Title="Diary"
    Width="1400"
    Height="800"
    AllowsTransparency="False"
    FontFamily="Fonts/#Inter"
    FontSize="13"
    Icon="Resources/Icon.ico"
    RenderOptions.BitmapScalingMode="HighQuality"
    RenderOptions.ClearTypeHint="Enabled"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:MainViewModel}">
            <views:MainView />
        </DataTemplate>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="24" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <controls:MainWindowControl VisibleViewModel="{Binding ElementName=Window, Path=DataContext}" />
</Window>
